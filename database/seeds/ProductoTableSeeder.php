<?php

use App\Producto;
use Illuminate\Database\Seeder;

class ProductoTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // factory(App\Producto::class, 5)->create()->each(function($u){
        //     $u->productos()->save(factory(App\Producto::class)->make());
        // });
    }
}
